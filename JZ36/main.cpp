#include <iostream>
#include <vector>
using namespace std;
struct ListNode{
    int val;
    struct ListNode *next;
    ListNode(int x):
        val(x),next(NULL){

        }
};
class Solution{
public:
    bool find(int target,vector<vector<int>> arry){
        vector<int> Num;
        ListNode* phead = head;
    }
    vector<vector<int>> CreateVector(void){
        vector<vector<int>> Num;
        for(int i = 0; i < 10;i++)
        {
            vector<int> tmp;
            for(int i = 0; i < 10; i++)
            {
                tmp.push_back()
            }
        }
        return head;
    }
};
int main()
{
    Solution a;
    ListNode *test = a.createList();
    a.printlist(test);
    return 0;
}

